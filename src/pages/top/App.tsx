// File Header: Top page application component.
import React from 'react'
import { layoutTheme } from '../../utils/Theme'

// Function Header: Renders placeholder text centered on the viewport.
export default function App(): React.ReactElement {
  return (
    <div className={layoutTheme.centeredContainer}>
      hoge
    </div>
  )
}
